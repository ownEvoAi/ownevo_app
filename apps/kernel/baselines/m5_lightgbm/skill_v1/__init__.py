"""Day-1 v1 skill bodies for the M5 baseline pipeline.

Each module here is a SKILL_FORMAT-compliant Python skill — its docstring
contains the YAML frontmatter required by `apps/kernel/.../skills/format.py`
and the rest is a focused stage of the forecasting pipeline.

The agent iterates these (W4 onward); v1 ships seasonal-naive bodies as
the Day-1 floor (see `../__init__.py` for the rationale).
"""
