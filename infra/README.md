# infra

Local development infrastructure — Langfuse self-hosted, Postgres, ClickHouse, OTel collector.

Empty until Week 1-2 trace pipeline work begins. Will land as `docker-compose.yml` plus per-service config.
